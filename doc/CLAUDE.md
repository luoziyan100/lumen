# doc/ — 宪法层

> [PROTOCOL] 设计决策(架构/视觉/协议语义)变更时:先在对应文档落笔并刷新下方状态表,再动实现;
> 完成后自检根 CLAUDE.md 的「铁律」引用是否仍准确。

职责:存放**深文档**——定义"什么是对的"的宪法与规范。此处不放过程稿(过程稿归 briefs/),不放目录地图(地图归各级 CLAUDE.md)。

## 文档状态表

| 文档 | 定位 | 状态 |
|---|---|---|
| `agent-core-architecture.md` | 架构宪法:不变式、拓扑、工具模型、验收纪律、里程碑 | **现行**(随里程碑持续回写) |
| `ui-design.md` | UI 宪法:产品形态(A)、设计原则、设计系统约束(§3 = 青瓷 v2) | **现行**(2026-07-04 追平实现;§5–6 mockup 为 6-11 原案,仅供溯源) |
| `ui-frontend-plan.md` | 前端形态 A 落地计划 | **已履约,存档**(现状以代码与 `packages/ui-client/CLAUDE.md` 为准) |

## 规则

- 每份文档头部必须有 `状态:` 行(现行 / 滞后待追平 / 已履约存档)。允许滞后,不允许滞后而不标。
- 其它文档与 CLAUDE.md **引用**宪法条款(给章节号),不复述——复述必漂移。
- `ui-design.md` §3 与 `packages/ui-client/src/tokens.css` 头部注释互为镜像:改其一必同步另一处。
