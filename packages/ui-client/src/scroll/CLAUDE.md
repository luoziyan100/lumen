# scroll/

> L2 | 父级: packages/ui-client/CLAUDE.md
> [PROTOCOL]: 变更时更新此头部,然后检查 CLAUDE.md

对话列贴底跟随。钉态外部 store,不重绘消息列。

## 成员

- `useStickToBottom.ts` — 流式贴底;上滑松钉;回滞再钉;高度回缩不追
- `scrollMsgAnchor.ts` — 可见消息锚点捕获/恢复
- `scrollDebug.ts` — ⌃⌥⇧S 诊断 HUD 的日志开关
