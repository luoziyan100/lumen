# env/vision/ — 识图

> L2 | 父级: `packages/agent-service/src/tools/env/CLAUDE.md`
> [PROTOCOL]: 变更时更新此头部,然后检查 CLAUDE.md

`look_at_image` 与图片侧车一起生死。

## 成员

- `vision-tools.ts` — `createLookAtImageTool` / `withImageSanitize`;env:`LUMEN_VISION_*`
- `image-store.ts` — 任务级侧车 + `[[image:img-N]]` 桩
- `index.ts` — 单元出口

法则: 成员完整·一行一文件·父级链接
