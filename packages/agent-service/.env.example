# 复制为 .env 并填入你的值;service 启动时自动读取(.env 不进 git)。
# 已在 shell 里设过的同名环境变量优先,.env 不会覆盖它。

LUMEN_PORT=8787
LUMEN_PROVIDER=openai
LUMEN_API_KEY=
LUMEN_BASE_URL=
TAVILY_API_KEY=
# LUMEN_MODEL=claude-opus-4-8
